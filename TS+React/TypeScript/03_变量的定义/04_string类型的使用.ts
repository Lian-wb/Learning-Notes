let message1: string = 'hello world'
let message2: string = "hello typescript"

// 开发情况下，一般不备注类型标识符，自动推导
// 如果一开始定义变量时还不确定具体数值，可以先加标识符防止类型错误
var name: string = "why"
let age: number = 18
const height: number = 1.88

let message3 = `name:${name} age:${age} height:${height}`

export {} 